{
  "name": "instituto-maos-que-ajudam",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Site institucional da ONG Instituto Mãos que Ajudam (Lajinha, MG)",
  "scripts": {
    "dev": "vite",
    "build": "vite build && html-minifier-terser --input-dir dist --output-dir dist --file-ext html --collapse-whitespace --remove-comments",
    "preview": "vite preview",
    "imagens": "node scripts/otimizar-imagens.mjs"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "sharp": "^0.35.4",
    "vite": "^8.3.0"
  }
}
